import TimerUI from '@/components/Timer_ui';
import { Link } from 'react-router-dom';

const TimeCountdown = () => {
  return (
    <div>
      <TimerUI/>
    </div>
  )
}

export default TimeCountdown;